<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Company | Sign In</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="h-full bg-gray-50 flex items-center justify-center min-h-screen">
    <div class="relative flex flex-col justify-center px-6 py-12 lg:px-8">
      <!-- Gradient Background -->
      <div
        class="absolute inset-0 -z-10 bg-gradient-to-r from-pink-500 via-purple-500 to-indigo-500 opacity-30 blur-3xl w-[500px] h-[500px] rounded-full m-auto"
      ></div>

      <!-- Card Box -->
      <div
        class="relative border p-6 rounded-xl bg-white shadow-xl w-fit min-w-[380px] z-10"
      >
        <div class="sm:mx-auto sm:w-full sm:max-w-sm">
          <p class="w-full text-center">-=|=-</p>
          <h2
            class="mt-5 text-center text-2xl font-bold tracking-tight text-gray-900"
          >
            Sign in to your account
          </h2>
        </div>

        <!-- Display general errors (authentication failures, server errors) -->
        <% if (errors._error) { %>
        <div class="mt-4 p-3 text-sm text-red-600 bg-red-100 rounded-md">
          <%= errors._error %>
        </div>
        <% } %>

        <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-sm">
          <form class="space-y-6" action="/auth/signin" method="POST">
            <div>
              <label for="email" class="block text-sm font-medium text-gray-900"
                >Email address</label
              >
              <div class="mt-2">
                <input
                  type="email"
                  name="email"
                  id="email"
                  autocomplete="email"
                  value="<%= formData.email || '' %>"
                  required
                  class="block border w-full rounded-md bg-white px-3 py-2 text-base text-gray-900 placeholder-gray-400 border <% if (errors.email) { %> border-red-500 <% } else { %> border-gray-300 <% } %>"
                />
                <% if (errors.email) { %>
                <p class="mt-1 text-sm text-red-600">
                  <%= errors.email._errors[0] %>
                </p>
                <% } %>
              </div>
            </div>

            <div>
              <label
                for="password"
                class="block text-sm font-medium text-gray-900"
                >Password</label
              >
              <div class="mt-2">
                <input
                  type="password"
                  name="password"
                  id="password"
                  autocomplete="current-password"
                  required
                  class="block w-full border rounded-md bg-white px-3 py-2 text-base text-gray-900 placeholder-gray-400 border <% if (errors.password) { %> border-red-500 <% } else { %> border-gray-300 <% } %>"
                />
                <% if (errors.password) { %>
                <p class="mt-1 text-sm text-red-600">
                  <%= errors.password._errors[0] %>
                </p>
                <% } %>
              </div>
            </div>

            <div>
              <button
                type="submit"
                class="flex w-full justify-center rounded-md bg-indigo-600 px-4 py-2 text-sm font-semibold text-white shadow-md hover:bg-indigo-500"
              >
                Sign in
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
